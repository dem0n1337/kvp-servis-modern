# SEO Status Report - kvp-servis.sk
**Dátum: 12. marca 2026**

---

## Stav indexácie v Google

| Stránka | Indexovaná | Poznámka |
|---------|-----------|----------|
| `/` (Domov) | Áno | Posledný cache: 15. december 2024 |
| `/sluzby` | Nie | Treba požiadať o re-indexáciu |
| `/kontakt` | Nie | Treba požiadať o re-indexáciu |
| `/fotogaleria` | Nie | Treba požiadať o re-indexáciu |

---

## Aplikácia (Nuxt.js) - STAV: VYRIEŠENÉ

Všetky SEO elementy sú správne implementované a SSR funguje korektne.

### Domov (`/`)
- `<html lang="sk">`
- `<title>` KVP-Servis - Profesionálne služby kúrenia, vody a plynu
- `<meta name="description">`
- `<link rel="canonical" href="https://kvp-servis.sk/">`
- Open Graph (og:title, og:description, og:url, og:type, og:locale, og:site_name)
- Twitter Card (twitter:card, twitter:title, twitter:description)
- Geo meta (geo.region, geo.placename, geo.position, ICBM)
- JSON-LD LocalBusiness structured data
- Heading hierarchy: h1 > h2 > h3

### Služby (`/sluzby`)
- Unique `<title>`: "Služby - KVP-Servis | Kúrenie, voda a plyn"
- Unique `<meta name="description">`
- Canonical URL, OG + Twitter tags

### Kontakt (`/kontakt`)
- Unique `<title>`: "Kontakt - KVP-Servis | Kúrenie, voda a plyn"
- Unique `<meta name="description">`
- Canonical URL, OG + Twitter tags

### Fotogaléria (`/fotogaleria`)
- Unique `<title>`: "Fotogaléria - KVP-Servis | Naše realizácie"
- Unique `<meta name="description">`
- Canonical URL, OG + Twitter tags
- 16 images with descriptive SEO alt texts

### Sitemap (`/sitemap.xml`)
- Generated by `@nuxtjs/sitemap`
- Contains all 4 pages: `/`, `/sluzby`, `/kontakt`, `/fotogaleria`
- HTTP 200, content-type: text/xml

### Robots.txt (`/robots.txt`)
- `User-Agent: * / Disallow:` (allow all)
- `Sitemap: https://kvp-servis.sk/sitemap.xml`
- HTTP 200, content-length: 68 bytes

### Structured Data (JSON-LD)
- `@type: LocalBusiness`
- Business name, phone, email
- Full address (Ružová 406/24, Slovenský Grob)
- GeoCoordinates (48.253643, 17.284890)
- OpeningHoursSpecification (Po-Ne 9:00-17:00)
- OfferCatalog with 3 categories (Kúrenie, Voda, Plyn)

---

## Cloudflare - STAV: VYRIEŠENÉ (12. marca 2026)

### Čo bolo opravené cez Cloudflare API:

| Nastavenie | Pred | Po | Vplyv |
|-----------|------|-----|-------|
| Bot Fight Mode | ON | **OFF** | Crawlery už nie sú blokované JS challenge-om |
| JS Detection | ON | **OFF** | Žiadne JS challenge pre boty |
| Managed robots.txt | ON | **OFF** | Slúži sa náš vlastný čistý robots.txt |
| Cache | Stale | **Purged** | Čerstvý obsah pre všetkých |

### Overenie po oprave:
- `sitemap.xml` → HTTP 200, text/xml, 1050 bytes
- `robots.txt` → HTTP 200, čistý bez Cloudflare managed content
- `/` → HTTP 200, text/html s plným SSR
- `/sluzby` → HTTP 200
- `/kontakt` → HTTP 200
- `/fotogaleria` → HTTP 200

### Existujúce Cloudflare nastavenia (ponechané):
- **Security Level:** Medium
- **Page Rules:** Cache everything + static assets caching + www→non-www redirect
- **WAF Custom Rule:** Challenge CN/RU/KP s threat score > 10
- **DDoS Protection:** Aktívna (Cloudflare managed)

---

## Zostávajúce kroky (manuálne)

### Google Search Console
1. Prihlásiť sa na [Google Search Console](https://search.google.com/search-console)
2. Pridať sitemap: `https://kvp-servis.sk/sitemap.xml`
3. Použiť **URL Inspection** pre každú stránku a požiadať o indexáciu
4. Skontrolovať, či Google vidí všetky meta tagy a structured data

### Google Business Profile
- Overiť, že profil je prepojený s `kvp-servis.sk`
- Pridať všetky služby a fotky z fotogalérie

### Monitoring
```bash
# Overenie dostupnosti SEO súborov:
curl -sI https://kvp-servis.sk/sitemap.xml | head -3
# Očakávané: HTTP/2 200, content-type: text/xml

curl -s https://kvp-servis.sk/robots.txt
# Očakávané: Náš čistý robots.txt s Sitemap referenciou
```
